#
# This is the version of the jenkins docker image that we inherit
#
export JENKINS_VERSION='2.130-slim'

#
# Bump this version when a change has been made to
# the build
#
export GSD_VERSION='v1.0.2-'

export GSD_JENKINS_VERSION="${GSD_VERSION}${JENKINS_VERSION}"
